export default {
        currentList(state) {
            return state.currentList;
        },
        componentIsLoaded(state) {
            return state.componentIsLoaded;
        },
        recoverBackEndIsBusy(state) {
            return state.recoverBackEndIsBusy;
        }
}